name: Deploy to Raspberry Pi

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Pi deploy
        run: |
          curl -sf ${{ secrets.PI_WEBHOOK_URL }} || echo "Pi webhook not reachable, will catch up on next poll"
